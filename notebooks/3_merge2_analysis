import pandas as pd
import json

# Load the UFO dataset (JSON format)
with open(r'C:\Users\opiej\ufo-analysis-capstone\ufo-analysis-capstone\data\ufo_sightings.json') as file:
    data = json.load(file)

# Flatten the nested JSON structure into a DataFrame
df_ufo = pd.json_normalize(data['rows'])
 